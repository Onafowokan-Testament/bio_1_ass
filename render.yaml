services:
  - type: web
    name: microscope-specimen-calculator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn phase_d_web.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
